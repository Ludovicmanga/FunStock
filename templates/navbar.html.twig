<nav class="main_nav_bar">
    {% if app.user %}
        <a href="{{ path('app_logout') }}">
            <li>Logout</li>
        </a>
    {% else %}
        <a href="{{ path('app_login') }}">
            <li>Login</li>
        </a>
    {% endif %}
    
    <a href="#">My stocks</a>
    <a href="#">Battles</a>
</nav>